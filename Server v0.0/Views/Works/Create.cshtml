@model Server_v0._0.Models.Work

@{
    ViewData["Title"] = "Добавить";
}

<h1>Добавить</h1>

<h4>Работа</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="StartDate" class="control-label"></label>
                <input asp-for="StartDate" class="form-control" />
                <span asp-validation-for="StartDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EndDate" class="control-label"></label>
                <input asp-for="EndDate" class="form-control" />
                <span asp-validation-for="EndDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="WorkersCount" class="control-label"></label>
                <input asp-for="WorkersCount" class="form-control" />
                <span asp-validation-for="WorkersCount" class="text-danger"></span>
            </div>
            <a class="form-group" asp-action="Create">
                <input type="submit" value="Добавить запись" class="btn btn-outline-success" />
            </a>
        </form>

        <div>
            <div class="border-top my-3"></div>
            <a class="form-group" asp-action="Index">
                <input type="submit" value="Вернуться назад " class="btn btn-outline-secondary" />
            </a>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
